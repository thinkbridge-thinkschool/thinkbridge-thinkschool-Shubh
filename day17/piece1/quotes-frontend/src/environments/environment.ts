// Development default. Matches the real QuotesApi backend run locally via
// `dotnet run` in day13/piece1/QuotesApi (see Properties/launchSettings.json).
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:5177',
};
